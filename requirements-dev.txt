# Local tooling only. Vercel installs requirements.txt, never this file
# (.vercelignore excludes it), so nothing here reaches the serverless bundle.
-r requirements.txt

pytest==9.1.1
pytest-django==4.11.1
gunicorn==26.1.0   # bench.py measures against a real WSGI server, not runserver
httpx==0.28.1      # bench.py HTTP client
ruff==0.16.3       # linting
